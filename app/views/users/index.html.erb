<h1><%= t 'views.common.all_users' %></h1>
<br>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, "/users/#{user.id}" %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>

<%= link_to t('devise.shared.links.edit'), '/users/edit' %>
<br>
<%= link_to Book.model_name.human, '/books' %>

